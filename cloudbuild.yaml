steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - functions
      - deploy
      - http-publisher-function
      - --region=us-central1
      - --runtime=java17
      - --source=.
      - --entry-point=com.gcp.labs.http.trigger.cloud.function.HttpTriggerFunction
      - --max-instances=1
      - --trigger-http
      - --vpc-connector=vpc-us-central1-connector
      - --egress-settings=all
      - --ingress-settings=internal-only
      - --service-account=$_SERVICE_ACCOUNT
      - --memory=128MB

options:
  logging: CLOUD_LOGGING_ONLY